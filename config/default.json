{
    "http": {
        "protocol": {{HTTP_PROTOCOL || "http"}},
        "host": "{{HTTP_HOST}}",
        "port": {{HTTP_PORT || 80}},
        "implementation": {
            "tag": "InMemory"
        }
    },
    "loglevel": "{{LOG_LEVEL}}",
    "jwtkey": "{{JWT_KEY}}"
}

