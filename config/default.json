{
    "databaseConfig": {
        "database": "{{DATABASE_NAME}}",
        "password": "{{DATABASE_PASSWORD}}",
        "port": {{DATABASE_PORT || 5432}},
        "user": "{{DATABASE_USER}}"
    },
    "httpServerConfig": {
        "host": "{{HTTP_HOST}}",
        "jwtKey": "{{JWT_KEY}}",
        "port": {{HTTP_PORT || 80}},
        "protocol": {{HTTP_PROTOCOL || "http"}}
    },
    "logLevel": "{{LOG_LEVEL}}",
    "salt": "{{SALT}}"
}

