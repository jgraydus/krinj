{
    "httpServerConfig": {
        "protocol": {{HTTP_PROTOCOL || "http"}},
        "host": "{{HTTP_HOST}}",
        "port": {{HTTP_PORT || 80}},
        "jwtKey": "{{JWT_KEY}}"
    },
    "logLevel": "{{LOG_LEVEL}}"
}

